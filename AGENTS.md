# Agent instructions (starlink / QuantDinger)

## Git

After you finish a **code or docs change** in this repository:

- **Commit** with a clear message; report the commit hash to the user.
- **Do not push** unless the user asks.

See `.cursor/rules/auto-commit.mdc` (always applied in Cursor).

## Code layout

- Backend API: `backend_api_python/`
- Agent API: `backend_api_python/app/routes/agent_v1/`
- Agent docs (English): `docs/agent/`
